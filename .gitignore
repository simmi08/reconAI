node_modules
.next
/storage/
*.log
.env
dist
coverage

.DS_Store
tsconfig.tsbuildinfo

# Keep historical raw samples in git as-is; ignore new local raw dumps.
/data_lake/raw/*

# Local synthetic datasets and loader script (do not commit)
/testdata/
/scripts/load-testset.sh
